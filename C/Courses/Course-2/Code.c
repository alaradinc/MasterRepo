#include <stdio.h>
#include <stdbool.h>

int main()
{
    // int num;
    // printf("Enter a number: ");
    // scanf("%d", &num);
    // printf("The number entered is: %d", num);

    // char gender;
    // printf("Enter sex: "); // I identify myself as an attack helicopter!!!!
    // scanf("%c", &gender); // 0x16FF1324A
    // printf("Your gender is: %c", gender);

    // int num = 5;
    // printf("The number is: %d\n", num);
    // printf("The address of the number is: %p", &num);

    // Ask user to enter 3 numbers and print the sum of the numbers

    // int a, b, c;
    // printf("Enter 3 numbers: ");
    // scanf("%d %d %d", &a, &b, &c);

    // printf("Your 3 numbers are: %d %d %d\n", a, b, c);
    // int sum;
    // sum = a + b + c;
    // printf("Sum is: %d", sum);

    // --------------------------------------------------------------------------------
    /*
        1. Arithmetic Operators
        + - * / %

    */

    // int a = 3, b = 2;

    // int addition = a + b;
    // printf("Addition: %d\n", addition);

    // int subtraction = a - b;
    // printf("Subtrction: %d\n", subtraction);

    // int multiplication = a * b;
    // printf("Multiplication: %d\n", multiplication);

    // float division = (float)a / b; // 3.0 / 2 ===> Typecasting
    // printf("Division: %f\n", division);

    // int remainder = a % b;
    // printf("Remainder: %d\n", remainder);

    // --------------------------------------------------------------------------------


    // int a = 5, b = 5, c = 5, d = 5;

    // // Pre-Increment
    // printf("Pre-Increment: %d\n", ++a); // 6

    // // Post-Increment
    // printf("Post-Increment: %d\n", b++); // 5

    // // Pre-Decrement
    // printf("Pre-Decrement: %d\n", --c); // 4

    // // Post-Decrement
    // printf("Post-Decrement: %d\n", d--); // 5
    
    // --------------------------------------------------------------------------------

    /*
        2. Relational Operators
        > < >= <= == !=
    */

    // int a = 5, b = 5;

    // printf("a > b: %d\n", a > b);
    // printf("a < b: %d\n", a < b);
    // printf("a >= b: %d\n", a >= b);
    // printf("a <= b: %d\n", a <= b);
    // printf("a == b: %d\n", a == b);
    // printf("a != b: %d\n", a != b);

    // --------------------------------------------------------------------------------

    /*
        3. Logical Operators
        && || !
    */

    // int a = 5, b = 5;

    // printf("a > b && a == b: %d\n", a > b && a == b);
    // printf("a > b || a == b: %d\n", a > b || a == b);
    // printf("!(a > b): %d\n", !(a > b));

    // --------------------------------------------------------------------------------


    printf("\n\n");
    return 0;
}
