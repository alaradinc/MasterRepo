import java.sql.SQLOutput;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        // int arr[5];
//        int[] arr = new int[5]; // int[] tells the program that this array will be of int type
//        String[] names = new String[5];

//        Scanner scan = new Scanner(System.in); // scan is an in between variable (think of it like cin in c++)
//
//        System.out.print("Input array length: ");
//        int lengthArr = scan.nextInt();
//
//        int[] array = new int[lengthArr]; //create a variable with the user inputted 'length' of indexes - defining how many boxes should be in the array
//
//        for (int i = 0; i < array.length; i++) { // must define array length otherwise it will continue post the determined index amt
//            System.out.print("Input index [" + i + "]: ");
//            array[i] = scan.nextInt(); //you can reuse scan because it is only a variable to temporarily get input from a source to assign it to another variable
//        }
//        System.out.println();
//
//        int sum = 0; // must define outside the loop because everytime a loop is over the sum will be redefined as 0
//
//        for (int i = 0; i < array.length; i++) {
//            System.out.println("array[" + i + "] => " + array[i]);
//            sum += array[i];
//        }
//        double avg = (float) sum / lengthArr; // must typecast otherwise two int divided will give an int (or you may redefine one of the variables as a float?double
//        System.out.println("Average = " + avg);
//
//        int min = array[0];
//        int max = array[0];
//
//        for (int i = 0; i < lengthArr; i++) {
//            if (array[i] < min) {
//                min = array[i];
//            }
//            if (array[i] > max) {
//                max = array[i];
//            }
//        }
//
//        System.out.println("Min = " + min + "\nMax = " + max);

//        int[][] mat = new int[3][3];
//
//        // X X X
//        // X X X
//        // X X X
//
//        for (int i = 0; i < mat.length; i++) {
//            for (int j = 0; j < mat[i].length; j++) {
//                System.out.print("mat[" + i + "][" + j + "]: ");
//                mat[i][j] = new Scanner(System.in).nextInt();
//            }
//            System.out.println();
//        }
//
//        for (int i = 0; i < mat.length; i++) {
//            for (int j = 0; j < mat[i].length; j++) {
//                System.out.print(mat[i][j] + " ");
//            }
//            System.out.println();
//        }

        
    }
}