package Intro;

public class Node { //we constructed a node and defined its inputs
    int data;
    Node next; //created a 'next' variable of Intro.Node type

    Node(int data){
        this.data = data;
        this.next = null;
    }
}
